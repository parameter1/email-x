#import '@parameter1/email-x-manage/gql/fragments/adunit/autocomplete'

query AdUnitAutocomplete($input: MatchAdUnitsQueryInput!) {
  matchAdUnits(input: $input) {
    edges {
      node {
        ...AdUnitAutocompleteFragment
      }
    }
  }
}
